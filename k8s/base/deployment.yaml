apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciudad-analitica
  namespace: ciudad-analitica
  labels:
    app.kubernetes.io/name: ciudad-analitica
    app.kubernetes.io/component: application
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: ciudad-analitica
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ciudad-analitica
        app.kubernetes.io/component: application
    spec:
      serviceAccountName: ciudad-analitica
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      containers:
        - name: app
          image: ciudad-analitica:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8501
              protocol: TCP
          env:
            - name: HOME
              value: /tmp
          envFrom:
            - configMapRef:
                name: ciudad-analitica-config
            - secretRef:
                name: ciudad-analitica-secrets
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
          startupProbe:
            httpGet:
              path: /_stcore/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /_stcore/health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /_stcore/health
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: app-data
              mountPath: /app/data
            - name: complex-chroma
              mountPath: /app/manuales_complejos_chroma_db
            - name: simple-chroma
              mountPath: /app/manuales_simples_chroma_db
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: app-data
          persistentVolumeClaim:
            claimName: app-data
        - name: complex-chroma
          persistentVolumeClaim:
            claimName: complex-chroma
        - name: simple-chroma
          persistentVolumeClaim:
            claimName: simple-chroma
        - name: tmp
          emptyDir:
            sizeLimit: 4Gi
