apiVersion: v1
kind: Service
metadata:
  name: chatbot
  namespace: chatbot
  labels:
    app.kubernetes.io/name: chatbot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: chatbot
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
