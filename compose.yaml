services:
  app:
    build:
      context: .
    image: chatbot:local
    init: true
    restart: unless-stopped
    env_file:
      - .env
    environment:
      CHATBOT_DB: /app/data/chatBot.sqlite3
      KNOWLEDGE_CHROMA_PERSIST_DIR: /app/knowledge_base_chroma_db
    ports:
      - "8501:8501"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8501/_stcore/health', timeout=3)"
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3
    volumes:
      - app-data:/app/data
      - knowledge-chroma:/app/knowledge_base_chroma_db

  api:
    build:
      context: .
    image: chatbot:local
    init: true
    restart: unless-stopped
    env_file:
      - .env
    environment:
      CHATBOT_DB: /app/data/chatBot.sqlite3
      KNOWLEDGE_CHROMA_PERSIST_DIR: /app/knowledge_base_chroma_db
    command: ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    volumes:
      - app-data:/app/data
      - knowledge-chroma:/app/knowledge_base_chroma_db

volumes:
  app-data:
  knowledge-chroma:
